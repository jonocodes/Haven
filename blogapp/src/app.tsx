export function App() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', margin: '2rem auto', maxWidth: 780, lineHeight: 1.5 }}>
      <h1>Blog App (remoteStorage)</h1>
      <p>
        Scaffold is now runnable. Core publish/index helpers live under <code>src/lib</code> and tests are under
        <code> src/tests</code>.
      </p>
      <ul>
        <li>Run <code>npm run dev</code> to start the app.</li>
        <li>Run <code>npm test</code> to run unit tests.</li>
      </ul>
    </main>
  )
}
